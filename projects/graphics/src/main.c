#include <ti/screen.h>
#include <ti/getcsc.h>
#include <stdlib.h>

/* Main function, called first */
int main(void)
{
    /* Clear the homescreen */
    os_ClrHome();

    //os_FillRectColor(OS_COLOR_RED);
    //check git on how to use gfx library

    /* Return 0 for success */
    return 0;
}
